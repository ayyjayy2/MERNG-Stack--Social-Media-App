//step 10a
module.exports = {
    //connection string from mongodb.com dashboard
    //add user1's password in place of <password>
    //change 'myFirstDatabase' to db name which we called it 'merng'
    MONGODB: 'mongodb+srv://user1:user1pwd1@cluster0.266jx.mongodb.net/merng?retryWrites=true&w=majority',

    //step 17f. key to encode token
    SECRET_KEY: 'some very secret key'

}